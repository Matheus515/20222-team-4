#include "funcionario.h"

class Pagamento {
    private:
        int _comissao;
        double _salario_final;
    public:
    Pagamento ();
    // calcular o salario no final do mes;
    double salario_final();
        
};